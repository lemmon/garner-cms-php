This is a **draft** (`"draft": true`) — it returns 404 publicly and never appears
in listings. Remove the flag (or set it to `false`) to publish it. Drafts are
still reachable through the query API with `page.children(drafts=true)`.
