This page lives at `routes/about/`. Its view is the co-located
`+template.twig`, which **extends** `app/templates/base.twig` — so one-off page
layouts stay next to their content while still reusing shared chrome.
