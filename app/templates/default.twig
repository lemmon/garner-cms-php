<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ page.title ?? 'Untitled' }} &middot; {{ site.title }}</title>
</head>
<body>
    <main>
        {% if page.title %}<h1>{{ page.title }}</h1>{% endif %}
        {{ content.main|markdown }}
    </main>
</body>
</html>
