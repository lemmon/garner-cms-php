<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ page.title }} | {{ site.title }}</title>
  </head>
  <body>
    <main>
      <h1>{{ page.title }}</h1>
      <p>{{ controller_message }}</p>
    </main>
  </body>
</html>
