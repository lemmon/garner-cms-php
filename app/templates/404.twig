<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{ error.title }} &middot; {{ site.title }}</title>
</head>
<body>
    <main>
        <h1>{{ error.title }}</h1>
        <p>No page matched <code>{{ error.path }}</code>.</p>
    </main>
</body>
</html>
